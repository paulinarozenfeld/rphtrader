<%= form_for note do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.text_area :body, cols: 20, rows: 10, class: "form-control" %>
  <%= f.submit "Add", class: "submit-note" %>
<% end %>
